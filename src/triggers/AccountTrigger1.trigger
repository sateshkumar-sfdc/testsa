//
//
//
trigger AccountTrigger1 on Account (before insert, before update, after insert, after update) {
    System.debug('-----------------------------------------Start Inside the AccountTrigger');

}