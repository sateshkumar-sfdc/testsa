//this is interface and we can use in other class without writing any method we can create a class
//abstraction
public interface RectangleInterface {
    Decimal area();
    Decimal area(Decimal l, Decimal b);
    Decimal perimeter();
    Decimal perimeter(Decimal l, Decimal b);

}